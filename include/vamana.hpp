#include <vector>
#include "../include/graph.hpp"

int Vamana(Graph &graph, vector<vector<double>> &coords, int R, double a, int int_L, int f); // added parameter f = label, to pass to generate_graph
                                                                                             // so that it creates a graph with nodes of label f.
